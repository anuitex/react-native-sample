import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    header: {
        padding: 10,
        alignItems: 'center',
    },
    text: {
        color: '#37B9F1',
        fontSize: 20,
        fontWeight: 'bold'
    },
})